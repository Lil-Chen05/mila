# Legacy: TriviaQA warm-up experiment

Retired scripts from the initial TriviaQA-based pipeline (plus early
scaffolding: `dummy.py`, `main.py`, `job.sh`). Superseded by the MMLU
checkpoint pipeline in `scripts/`. Kept for reference; not maintained.
Its dataset lives (untracked) in `data/trivia_50/`.
